package Main.Items;

/**
 * Oklart hur Main.Items.Trap ska fungera. Behöver tänka mer på hur fällor passar in i spelet.
 */

public class Trap extends RoomItem {

    Trap(String itemName, String description) {
        super(itemName, description);
    }
}
